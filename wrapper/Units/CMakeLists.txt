########################################
#
# CMake file for Python wrapper:_Units
#
########################################

# Third Party dependencies of this module
include_directories( ${BOOST_INCLUDE_DIR}
                     ${PYTHON_INCLUDE_DIR}
                     ${SIRE_GSL_INCLUDE_PATH} )

# Sire include paths
include_directories( BEFORE ${SIRE_INCLUDE_DIR}/SireUnits
                            ${SIRE_INCLUDE_DIR} )

# Other python wrapping directories
include_directories(${CMAKE_SOURCE_DIR})

# Get the list of autogenerated files
include(CMakeAutogenFile.txt)

# Define the sources in Units
set ( WRAPUNITS_SOURCES

      _Units.main.cpp
     
      generalunit.cpp
      sireunits_dimensions.cpp
      _Units_global_variables.pyman.cpp

      ${PYPP_SOURCES}

    )

# Create the library that holds all of the class wrappers
add_library (Units ${WRAPUNITS_SOURCES})

set_target_properties (Units
                       PROPERTIES
                         VERSION ${SIRE_VERSION}
                         SOVERSION ${SIRE_VERSION_MAJOR}
                         OUTPUT_NAME "Units"
                         PREFIX "_"
                         SUFFIX ${PYTHON_MODULE_EXTENSION}
		       )
		       
target_link_libraries (Units
                       SirePython
                       SIRE_SireUnits
                       SIRE_SireBase
                       ${BOOST_PYTHON_LIBRARY}
                       ${PYTHON_LIBRARIES}
                      )

include( LimitSirePythonExportSymbols )
export_this_symbol_only( "init_Units" "_init_Units" )

# installation
set( INSTALLDIR ${SIRE_PYTHON}/Sire/Units )

install( TARGETS Units
         LIBRARY DESTINATION ${INSTALLDIR}
       )

install( FILES __init__.py
         DESTINATION ${INSTALLDIR} 
       )

